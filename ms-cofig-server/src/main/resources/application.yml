server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/abdul8377/DAD-gitclone.git # ruta del repositorio git as
          searchPaths: config-data
          default-label: master
  security:
    user:
      name: root
      password: 123456